# New Projects

This folder hosts standalone migration target projects created inside the same repository.

Use this mode when you want to avoid contaminating the legacy codebase and build the new bounded-context implementation as a separate project with explicit integration seams.
